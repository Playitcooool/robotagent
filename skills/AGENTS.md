# Skills Guide

This folder is reserved for project-specific skill or agent capability material.

Before adding files here, check whether the content belongs in runtime prompts (`prompts/`), tool code (`tools/`), docs (`docs/`), or a Codex skill outside the repo. Keep any skill instructions tightly scoped and avoid duplicating root `AGENTS.md` guidance.
