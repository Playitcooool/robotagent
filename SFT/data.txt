初始化模拟环境，并在 [0.2, 0.0, 0.02] 放置一个立方体，返回最终状态摘要。
在已初始化环境中执行 push_cube_step：从 [0.2, 0.0, 0.02] 向 +x 方向推 0.3m，输出最终位置与速度。
将方块从 [0.2, 0.0, 0.02] 逐步推到接近 [0.5, 0.0, 0.02]，每 20 步汇报一次进度。
在仿真中把方块从 [0.2, 0.0, 0.02] 推向 [0.5, 0.5, 0.02]，并在结束时给出轨迹要点。
使用机器人臂抓取位于 [0.2, 0.0, 0.02] 的方块并放置到 [0.45, 0.35, 0.02]。
执行一次抓取放置任务：起点 [0.25, 0.1, 0.02]，终点 [0.5, -0.2, 0.02]，返回执行结果。
请规划从 [0.2, 0.0, 0.02] 到 [0.5, 0.5, 0.02] 的离散路径点，路径点数设为 12。
给我一个从 [0.3, -0.2, 0.02] 到 [0.6, 0.2, 0.02] 的路径规划结果，并说明是否平滑。
先调整物理参数到摩擦 0.4、弹性 0.7，再推动方块并对比调整前后的位移差异。
将摩擦设为 0.2、弹性 0.9，运行 200 步，汇报最终位姿和是否稳定。
把摩擦从 0.8 降到 0.2，分别执行同样的推送任务，比较结果。
创建 3 个方块，分别从不同起点抓取并放置到目标区域 [0.55, 0.15, 0.02] 附近。
多物体抓取放置：起点列表 [[0.15,0.0,0.02],[0.2,0.1,0.02],[0.25,-0.1,0.02]]，终点 [0.5,0.3,0.02]。
请执行 multi_object_grab_and_place，并汇报每个物体是否成功到达目标。
调用视觉传感器模拟，返回图像并说明画面中主要物体位置。
在推动方块的过程中每 50 步采样一次视觉图像，最后总结运动趋势。
先 check_static_assets，再初始化仿真；若资源缺失请明确指出并停止执行。
初始化后立刻调用 check_simulation_state，确认是否已有 plane 与目标物体。
执行任务前先检查当前仿真状态，如果已有旧对象先清理再开始。
连续执行两次推送任务，中间不重置，比较第二次是否受第一次残留状态影响。
执行一次任务后 cleanup，再重跑同任务，比较两次结果一致性。
创建方块后等待 120 步不施加外力，判断系统是否稳定并解释依据。
把方块从 [0.2,0,0.02] 推到 [0.6,0,0.02]，若偏差超过 0.05m 给出可能原因。
对同一推送任务运行 3 次，汇总平均最终位置与方差。
路径规划后不要直接执行，只输出计划步骤与可行性评估。
先给出 3 步计划，再执行抓取放置，并在每步完成后更新状态。
在没有给目标位置的情况下，请先询问我目标，再继续执行。
我只说“帮我做个推送仿真”，你需要先补充必要参数再执行。
如果我没有给摩擦和弹性，请使用默认值并明确告诉我默认参数。
请在执行前先说明你将调用哪些工具，然后再开始。
给我一个最小可复现仿真流程：初始化、推送、状态检查、清理。
执行推送任务并把关键结果以“结论/关键结果/下一步”三行输出。
抓取任务失败时不要编造结果，直接返回失败原因和恢复建议。
如果目标点不可达，请返回“不可达”并给出替代目标建议。
在运行过程中如果检测到异常速度峰值，请提前终止并上报。
先执行 path_planning，再依据路径分段执行 push/抓取动作并汇总。
将目标位置改成 [0.9, 0.9, 0.02]，判断是否超出安全工作区并给建议。
请把仿真任务拆成 4 步，并持续更新每步状态到完成。
执行任务后输出一段简洁中文总结，不要输出大段 JSON。
执行视觉传感器后给出可点击的图像引用（如果有 URL/路径）。
用默认参数跑一次，再用摩擦 0.9 弹性 0.1 跑一次，比较差异。
给我一个“最保守稳定”的物理参数组合并解释为什么。
模拟弹跳过程：弹性 0.95、摩擦 0.1，观察 300 步后是否停止。
模拟高摩擦低弹性：摩擦 0.9、弹性 0.05，预期会发生什么并验证。
执行一次多物体任务，并输出每个物体的最终坐标列表。
执行抓取放置后，调用 check_simulation_state 验证结果是否一致。
如果当前 simulator 不可用，请不要继续假设执行，直接提示我启动依赖。
我给你一个模糊请求“做个机器人实验”，请先给我参数清单供选择。
你需要在回答末尾附上本次调用的工具列表。
给我一次从初始化到清理的完整闭环，并确认资源已释放。
连续发送两条命令：第一条只规划，第二条才执行；你现在先只规划。
请只做状态检查，不要执行任何会改变环境状态的操作。
请重置世界后再执行推送任务，最后确认 reset 是否生效。
请在任务结束后主动 cleanup，并返回 cleanup 成功标志。
把方块推进到目标后，再执行一次视觉采样并总结是否到位。
对比 grab_and_place_step 与 push_cube_step 的结果差异，给出适用场景。
同样目标下，优先选择更稳定的工具并解释你的选择依据。
执行任务时如果需要我确认（例如 reset/delete），先停下来询问我。
给我一个“失败注入”实验：故意给不可达目标，观察系统如何处理。
给我一个“恢复实验”：先失败一次，再修正参数并成功完成任务。
请验证在只输入中文口语化指令时，系统是否仍能正确路由工具。
请验证在中英混合指令下，系统是否能稳定执行并给出中文总结。
在初始化环境后，先读取当前状态，再决定是否需要放置新物体，最后说明你的判断依据。
请把推送任务拆成“准备-执行-验证-清理”四阶段，并在每阶段结束给出一句状态。
我希望你用最少工具调用完成把方块移动到 [0.45, 0.0, 0.02]，并解释为什么这样更高效。
同一目标下分别用 push 和 grab 两种策略执行，比较成功率和稳定性。
请先做路径规划，不执行动作，只输出路径点和风险点。
先把摩擦设为 0.1 执行推送，再改成 0.9 重复执行，对比两次最终误差。
在目标点附近设置容差 0.02m，判断任务是否达标并给出结论。
如果最终位置偏差超过容差，请自动给出一次参数修正并重试。
在重试前先说明你将修改哪些参数以及预期影响。
请执行一次“短步长高频”推送，再执行一次“长步长低频”推送并比较。
模拟一个需要中途暂停再继续的任务，分别汇报暂停前后状态。
先执行 100 步，再检查状态；若速度仍较高，再追加 80 步直到稳定。
给我一个能复现实验结果的最小参数清单（位置、步数、摩擦、弹性）。
你需要在输出里给出 run_id 或可追踪标识，便于我复现。
请在执行前确认当前是否存在旧会话残留，若有先 cleanup。
执行完成后请检查是否仍有非必要对象，若有请删除并确认。
模拟把两个物体依次移动到不同目标：[0.45,0.2,0.02] 与 [0.55,-0.2,0.02]。
先多物体抓取，再单物体精调，最后验证所有物体位置是否达标。
请验证视觉采样返回的图像时间戳是否晚于任务开始时间。
如果视觉图像缺失，请不要编造，直接报告缺失并给排查建议。
对一次任务输出“关键数值摘要”：初始位置、最终位置、位移、是否完成。
请把回答控制在 6 行以内，但要包含结论和关键数据。
执行时若遇到工具超时，请重试一次并记录重试原因。
若重试仍失败，请停止并返回“失败+建议”，不要继续调用其他危险工具。
请展示一次“仅检查不执行”的 dry-run，列出计划调用工具序列。
我要求你不要调用 reset_world，只能在当前状态下完成任务。
我要求你必须先 reset_world 再执行任务，并在结果里确认 reset 生效。
在抓取任务前先检查目标是否可达；若不可达，改为最近可达点并说明。
把方块移动到 [0.5,0.5,0.02] 后，再移动回 [0.2,0.0,0.02]，比较往返误差。
执行一个闭环控制风格的任务：每 30 步检查一次偏差并微调动作。
给出一次“参数扫描”实验：摩擦 [0.2,0.5,0.8]，比较三组结果。
给出一次“弹性扫描”实验：弹性 [0.1,0.5,0.9]，比较反弹高度趋势。
如果用户请求不明确，请先提出 3 个澄清问题再执行。
请把你最终用到的默认参数单独列成一行，便于记录。
如果要调用会改变状态的操作，请先请求我的确认再执行。
请模拟一个任务失败后恢复成功的完整流程，并标注失败点与恢复点。
先执行仿真，再用简短分析总结“哪一步最影响结果”。
请给出本次任务的可复现实验脚本思路（不要写具体代码）。
