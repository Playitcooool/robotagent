# Charts Guide

This folder contains chart-generation code and produced figures.

- `generate_charts.py` is the main chart creation script.
- `figures/` holds generated image assets.

Keep chart scripts reproducible and prefer regenerating figures from source data over manual edits. If changing visual style, check downstream documents or README references that may embed these figures.
