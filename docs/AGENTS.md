# Docs Guide

This folder contains supporting project documentation, including deeper notes under `superpowers/`.

Use docs for durable explanations that are useful to future humans. Keep runnable setup and API details synchronized with `README.md`, `config/config.yml`, and the actual backend/frontend entry points.

Do not describe old model names, ports, or scripts as fixed truth if the current code resolves them from configuration.
