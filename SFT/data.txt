# 统计：共 87 条 prompt，20 个类别
# 基础搬运 4 | 路径规划 4 | 物理参数 6 | 多物体 5 | 视觉反馈 4 | 环境检查 4
# 完整实验 4 | 稳定性测试 4 | 分步控制 5 | 安全约束 4 | 异步等待 3 | 错误恢复 5
# 对比实验 4 | 工具优化 3 | 状态管理 3 | 往返误差 3 | 口语混合 2 | 仿真分析 3
# Gazebo 13 | 跨平台 4

# 基础搬运任务
帮我把立方体从 [0.2, 0, 0.02] 推到 [0.5, 0, 0.02]，输出最终位置。
把方块推到右边 0.3 米的位置，然后告诉我结果。
抓取位于 [0.2, 0, 0.02] 的物体并放到 [0.45, 0.35, 0.02]。
把东西从这边拿到那边去，起点 [0.25, 0.1, 0.02]，终点 [0.5, -0.2, 0.02]。

# 路径规划
帮我规划一下从 [0.2, 0, 0.02] 到 [0.5, 0.5, 0.02] 的路径，并检查是否平滑。
从 [0.3, -0.2, 0.02] 到 [0.6, 0.2, 0.02] 做路径规划，输出路径点和可能的风险点。
先规划路径，再按路径移动物体，只规划不执行。
根据规划的路径，将物体从起点移动到终点并验证最终位置。

# 物理参数调整
把摩擦改成 0.4、弹性改成 0.7，然后推送方块观察效果并汇报稳定状态。
设置摩擦 0.2、弹性 0.9，运行 200 步后告诉我稳定后的状态。
分别用摩擦 [0.2, 0.5, 0.8] 各推一次方块，对比最终位置的差异。
分别用弹性 [0.1, 0.5, 0.9] 各推一次方块，对比弹跳停止后的状态差异。
摩擦从 0.8 改到 0.2，分别推送一次，对比有什么区别。
用默认物理参数运行一次，告诉我默认的摩擦和弹性值是多少。

# 多物体操作
创建 3 个方块，分别抓取放到 [0.55, 0.15, 0.02] 附近并验证位置。
多物体操作：把三个东西放到 [0.5, 0.3, 0.02]，然后告诉我每个的最终位置。
两个物体依次移动到不同目标：[0.3, 0, 0.02] 和 [0.6, 0.2, 0.02]。
先创建多个物体，抓取后精调位置，验证所有物体的最终位置。
检查当前环境有多少个物体，清理掉旧的，再重新创建新的。

# 视觉反馈
初始化仿真环境后拍一张照片，告诉我画面里有什么。
每 50 步拍一次照，观察物体移动过程并汇报观察到的情况。
把方块推到目标 [0.5, 0, 0.02]，然后拍照验证是否到位。
连续拍照记录物体从起点到终点的运动轨迹，输出图像链接。

# 环境检查
检查仿真环境里有没有缺少什么资源，告诉我当前世界状态。
读取当前仿真世界的完整状态，包括所有物体位置和仿真参数。
先读取当前状态，判断要不要放新东西，如果需要就创建物体。
有旧的物体就先清理掉，然后创建一个新方块在 [0.3, 0, 0.02]。

# 完整实验流程
做一次完整的推送实验：初始化仿真、推送方块、检查最终位置、清理环境。
从初始化到清理完整跑一遍，确认资源完全释放，并汇报整个流程。
连续推两次同样的目标，记录第二次是否受到第一次的影响。
连续推三次同一个目标，求平均位置和位置方差。

# 稳定性测试
设置弹性 0.95、摩擦 0.1，观察弹跳 300 步后是否停止并报告最终状态。
推到目标 [0.6, 0, 0.02]，如果偏差超过 5cm 分析原因并给出修正建议。
等待 120 步不加力，观察系统是否稳定并报告位置漂移量。
同一任务做 3 次，记录每次的初始位置、最终位置和位移量。

# 连续控制与分步执行
把"把方块推到 [0.5, 0, 0.02]"这个任务分成四步来做，每步完成后告诉我状态。
分三步把方块推到 [0.5, 0, 0.02]：先规划、中途微调、最终验证，每步汇报状态。
先做什么再做什么最后做什么，给我一个把物体从 [0.2,0,0.02] 推到 [0.5,0.5,0.02] 的三步计划并执行。
执行任务中途暂停，报告当前物体位置，然后继续执行直到完成。
每 30 步检查一次偏差，如果超过 2cm 就自动微调，直到到达目标。

# 安全约束
判断目标点 [0.9, 0.9, 0.02] 是否超出安全范围，如果超出给出最近的有效目标。
把方块推到 [0.8, 0, 0.02]，但约束不能超过 [0.7, 0.5, 0.1] 这个范围。
抓取前检查目标 [0.4, 0.5, 0.02] 是否可达，如果不可达用最近的可达点替代。
判断给定的多个目标点 [0.3,0,0.02]、[0.6,0.3,0.02]、[0.9,0,0.02] 哪些可达并排序。

# 异步与等待
开始推送物体后等待其稳定，然后检查最终位置并汇报。
执行推送任务后验证最终位置是否在目标 3cm 范围内。
完成抓取和放置任务后，主动清理仿真环境中的所有物体。

# 错误处理与恢复
执行推送任务，如果失败了告诉我真正的原因，不要编造。
推送失败后尝试用抓取的方式重新完成任务，如果仍失败给出建议。
给一个达不到的目标 [1.0, 1.0, 0.02]，观察系统如何反应并报告。
任务执行失败后，记录失败原因，执行重置，然后重试一次。
先故意让任务失败一次，然后修正参数再执行，记录完整的失败和恢复流程。

# 对比实验
用 push 方式和 grab 方式各执行一次把物体从 [0.2,0,0.02] 推到 [0.5,0,0.02] 的任务，对比哪个成功率更高。
用默认物理参数推一次，再用摩擦 0.9、弹性 0.1 的参数推一次，对比两次的位移误差。
短步长高频（步长0.01，共100步）和长步长低频（步长0.05，共20步）各推一次，对比哪个更精准。
对比 push 和 grab 两种操作方式在同一目标 [0.4, 0, 0.02] 下的稳定性差异。

# 工具选择与优化
设计一个最优的推送实验，参数由你决定，目标是把物体移动 0.3 米并尽量精准。
用最少的工具调用次数完成把物体从 [0.2,0,0.02] 移动到 [0.5,0,0.02] 的任务。
先只做路径规划，不执行，输出完整的移动计划。

# 状态管理
在当前仿真状态下完成任务，不能调用 reset_world，记录当前状态。
先 reset 仿真世界，再执行把物体推到 [0.5, 0, 0.02] 的任务，确认重置生效。
记住当前物体位置，然后添加一个新物体，稍后回来继续操作原来的物体。

# 往返与误差分析
把物体从 [0.2, 0, 0.02] 推到 [0.5, 0, 0.02] 再推回来，测量往返误差。
目标附近允许 2cm 容差，判断把物体推到 [0.51, 0.02, 0.02] 是否在容差范围内。
把物体推到目标位置，如果偏差超过 2cm 自动修正重试一次，直到成功。

# 口语化与混合指令
用中文口语"帮我把这个方块弄到右边去"指令执行搬运任务。
执行中英混合指令"push the cube to [0.5, 0, 0.02]"并报告结果。

# 仿真后分析
仿真后分析摩擦和弹性参数对位移精度的影响程度，给出参数选择建议。
设计一个可复现的实验流程，验证把物体推送到目标位置 [0.5, 0.5, 0.02] 的精度。
对比 PyBullet 和 Gazebo 两种仿真环境在同一推送任务下的结果差异。

# Gazebo 基础操作
启动 ROS 连接并检查是否正常，确认后报告连接状态。
在 Gazebo 里创建一个立方体在 [0, 0, 0.5]，创建后验证物体是否成功生成。
在 Gazebo 中创建一个机器人模型，获取其初始位姿并报告。
当前 Gazebo 仿真环境中有哪些模型在运行，列出所有模型名称和位置。
暂停 Gazebo 仿真，移动模型到 [1, 0, 0.5]，然后恢复仿真验证效果。
重置 Gazebo 仿真到初始状态，确认所有模型都回到世界原点。
在 Gazebo 中拍照，记录当前仿真环境的视觉状态。
删除指定的 Gazebo 模型，然后立即创建一个新模型，验证替换操作是否成功。
在 Gazebo 中批量创建 5 个模型，确认所有模型都正常生成并获取它们的位置。
 Gazebo 仿真过程中持续监控某个模型的位置变化，记录运动轨迹。
 Gazebo 仿真后拍照对比执行前后的差异，报告物体位置变化。
完成 Gazebo 操作后清理 ROS 连接，确认资源释放。
 Gazebo 中连续创建和删除模型各 5 次，测试系统稳定性并报告结果。

# 跨平台对比
用 PyBullet 把方块从 [0.2, 0, 0.02] 推到 [0.5, 0, 0.02]，记录精度。
用 Gazebo 做同样的推送任务，对比两者的最终位置误差。
在 Gazebo 环境里创建机器人并执行抓取放置任务到指定位置。
对比 PyBullet 和 Gazebo 在相同推送任务下的精度差异，给出分析报告。
